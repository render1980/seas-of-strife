{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "init-db": "bun run scripts/init-db.ts",
    "dev": "bun --watch index.ts",
    "fmt": "npx prettier --write '**/*.ts'",
    "test": "bun test tests/game/*",
    "test:it": "bun run --env-file .env.test scripts/init-db.ts --create seas_of_strife_test && bun test --preload ./tests/it/helpers/setup.ts tests/it/; code=$?; bun run --env-file .env.test scripts/drop-test-db.ts; exit $code"
  },
  "dependencies": {
    "postgres": "^3.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
