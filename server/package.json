{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "init-db": "bun run scripts/init-db.ts",
    "dev": "bun --watch index.ts",
    "fmt": "npx prettier --write '**/*.ts'"
  },
  "dependencies": {
    "postgres": "^3.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
